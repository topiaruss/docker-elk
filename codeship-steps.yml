- service: elasticsearch
  command: echo builtES
- service: elasticsearch
  type: push
  image_name: topiaruss/elasticsearch
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: logstash
  command: echo builtLS
- service: logstash
  type: push
  image_name: topiaruss/logstash
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: kibana
  command: echo builtKB
- service: kibana
  type: push
  image_name: topiaruss/kibana
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: filebeat
  command: echo builtFB
- service: filebeat
  type: push
  image_name: topiaruss/filebeat
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: filebeatcass
  command: echo builtFBC
- service: filebeatcass
  type: push
  image_name: topiaruss/filebeatcass
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

