- service: elasticsearch
  command: echo builtES
- service: elasticsearch
  type: push
  image_name: topiaruss/elasticsearch
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: logstash
  command: echo builtLS
- service: logstash
  type: push
  image_name: topiaruss/logstash
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted

- service: kibana
  command: echo builtKB
- service: kibana
  type: push
  image_name: topiaruss/kibana
  registry: https://index.docker.io/v1/
  encrypted_dockercfg_path: dockercfg.encrypted
